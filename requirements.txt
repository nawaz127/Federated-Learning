# ==============================================================
#  Federated Learning for Medical Image Classification
#  Runtime Dependencies
# ==============================================================

# ---------- Deep Learning Core ----------
torch>=2.0
torchvision>=0.15
timm>=0.9                   # ViT, Swin-Tiny, hybrid models
tensorboard                 # training curve logging

# ---------- Federated Learning ----------
flwr>=1.5                   # Flower framework (FedAvg / FedProx)
grpcio                      # gRPC transport (used directly in client)

# ---------- Data Science / Numerical ----------
numpy>=1.24
pandas>=2.0
scipy>=1.10

# ---------- Visualization ----------
matplotlib>=3.7
seaborn>=0.12

# ---------- Computer Vision / Image Processing ----------
opencv-python>=4.8          # CLAHE, color-space conversion
albumentations>=1.3         # augmentation pipeline + ToTensorV2
Pillow>=9.0                 # indirect dep (torchvision, matplotlib)
scikit-image>=0.21          # SSIM, mark_boundaries (XAI)

# ---------- Machine Learning Metrics ----------
scikit-learn>=1.3           # accuracy, confusion matrix, AUC, etc.

# ---------- XAI / Explainability ----------
lime>=0.2
shap>=0.42

# ---------- Utilities ----------
tqdm>=4.65                  # progress bars
